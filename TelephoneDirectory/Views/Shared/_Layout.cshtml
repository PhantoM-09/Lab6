<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>_Layout</title>
</head>
<body>
    <div>
        <header>
            @if (IsSectionDefined("Header"))
            {
        @RenderSection("Header") }
    else
    {
        <span>Header по умолчанию</span>            }
        </header>


        @RenderBody()

        <footer>
            @if (IsSectionDefined("Footer"))
            {
@RenderSection("Footer") }
            else
            {
<span>Footer по умолчанию</span>            }
        </footer>
    </div>
</body>
</html>
